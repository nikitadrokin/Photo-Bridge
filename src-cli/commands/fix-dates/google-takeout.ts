/**
 * Reserved for Google Takeout sidecar handling (GPS, JSON times). Currently unused.
 */
export async function applyGoogleTakeoutEnhancements(
  _mediaPath: string,
): Promise<void> {
  // Intentionally empty until Takeout logic is moved here from the batch/apply paths.
}
